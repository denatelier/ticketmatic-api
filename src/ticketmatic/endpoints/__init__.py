"""API endpoint functions for the Ticketmatic API.

Each submodule groups the operations for one part of the API as module-level
functions that take a :class:`~ticketmatic.client.Client` as their first
argument, for example ``from ticketmatic.endpoints import events``.
"""
